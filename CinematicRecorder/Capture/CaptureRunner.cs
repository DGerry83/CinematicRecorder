using UnityEngine;

namespace CinematicRecorder.Capture
{
    /// <summary>
    /// MonoBehaviour host for capture coroutines. Attach to a GameObject to run capture sessions in the Unity lifecycle.
    /// </summary>
    public sealed class CaptureRunner : MonoBehaviour
    {
    }
}